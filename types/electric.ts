export type ElectricBody = {
    deviceId?: unknown
    from?: unknown
    to?: unknown
}

export type ValidatedElectricBody = {
    deviceId: string
    from: string
    to: string
}

export type ExportExcelBody = {
    deviceId?: unknown
    from?: unknown
    to?: unknown
}

export type ValidatedExportExcelBody = {
    deviceId: string
    from: string
    to: string
}